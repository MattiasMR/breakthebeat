---
interface Props {
  prefix: "captain" | "partner";
  title: string;
}

const { prefix, title } = Astro.props;
---

<article class="medical-panel" data-medical-panel={prefix}>
  <div class="form-card-heading">
    <h3>{title}</h3>
  </div>
  <div class="emergency-block">
    <div class="field-grid">
      <label class="field">
        <span>Nombre completo <b aria-hidden="true">*</b></span>
        <input name={`${prefix}.medical.emergencyName`} required minlength="2" maxlength="120" placeholder={prefix === "captain" ? "José Eduardo" : "María Tornillo"} />
      </label>
      <label class="field">
        <span>Relación <b aria-hidden="true">*</b></span>
        <select name={`${prefix}.medical.emergencyRelationship`} required>
          <option value="">Seleccionar</option>
          <option>Padre</option>
          <option>Madre</option>
          <option>Hermano/a</option>
          <option>Pareja</option>
          <option>Amigo/a</option>
          <option>Otro</option>
        </select>
      </label>
      <label class="field">
        <span>Teléfono <b aria-hidden="true">*</b></span>
        <input name={`${prefix}.medical.emergencyPhone`} type="tel" autocomplete="tel" required minlength="8" maxlength="24" pattern="[+0-9][0-9 ()-]{7,22}" aria-describedby={`${prefix}-emergency-phone-error`} title="Usa entre 8 y 23 caracteres: números, espacios, paréntesis o guiones" placeholder="0999999999" />
        <small id={`${prefix}-emergency-phone-error`} class="field-validation-error" data-field-validation-error hidden></small>
      </label>
    </div>
  </div>
</article>
